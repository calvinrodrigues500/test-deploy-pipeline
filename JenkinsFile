pipeline {
	agent: any
	
	stages {

		stage('Build') {
			echo "I am building the project"
		}

		stage('Testing') {
			echo "I am testing the project"
		}

		stage('Deploy') {
			echo "I am deploying the project"
		}
	}
}